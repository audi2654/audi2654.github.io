Everything is in HTML file.

The contact form also & its script also. 

Do not change id of form.

Do not change regular expressions verifying the data such name, phone,etc in script. Any other regex doesn't work easily.

Mobile Browser compatible tooltips added.